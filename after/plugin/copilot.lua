require('copilot').setup({
  suggestion = {
    enabled = true,
    auto_trigger = true,
    accept = false,
  },
  panel = {
    enabled = true,
    auto_refresh = true
  },
})
-- require('copilot_cmp').setup()
